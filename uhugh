#include "iostream"

int main() {
	const int n = 10;
	int nums[n];
	for (int i = 0; i < n; i++) {
		std::cin >> nums[i];
	}
}
for (int i = 0; i < n;; i++) {
	if (nums[i] % 2 == 0 && nums[i] % 3 == 0) {
		std::cout << nums[i] << std::endl;
	};
}
